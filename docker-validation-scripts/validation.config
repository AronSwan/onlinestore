# Validation configuration
# Compose layering order and environment mapping
# ENV can be one of: dev, test, staging, prod
ENV=dev
# ORDER defines layering: base,env,override,others
ORDER=base,env,override