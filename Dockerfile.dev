# 前端开发环境 Dockerfile
FROM node:18-alpine

# 安装开发工具
RUN apk add --no-cache git

# 设置工作目录
WORKDIR /app

# 复制 package 文件
COPY package*.json ./

# 安装依赖 (包括开发依赖)
RUN npm install

# 复制源代码
COPY . .

# 暴露端口
EXPOSE 3000

# 开发命令
CMD ["npm", "run", "dev"]